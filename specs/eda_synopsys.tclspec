# tcl-lsp — bundled SpecTcl loadable: the Synopsys tool commands (Design Compiler, IC Compiler, PrimeTime).
#
# This pack IS the source of truth for these commands. The Rust modules that
# used to hold them (rust/tcl-registry/src/commands/eda_synopsys/) were deleted when
# the EDA vendor libraries migrated to bundled `.tclspec` loadables, per
# docs/design/spec-packs.md ("the EDA vendor libraries ship as bundled
# `.tclspec` loadables … so the loader path is exercised in production from day
# one rather than reserved for private packs"). Every command was proved
# field-for-field equal to its compiled spec by the SpecTcl round-trip gate
# before the modules were removed.
#
# Discovery: the **bundled** tier — `specs/` beside the server executable, or
# wherever $TCL_LSP_SPEC_PACK_DIR points (rust/tcl-spectcl/src/discovery.rs).
# Edit this file to change the specs; there is no generator to re-run.
# Syntax: docs/design/spec-dsl-examples/README.md. The initial text was written
# by the spec studio's SpecTcl renderer (rust/tcl-spec-studio/src/render_spectcl.rs)
# from the registry modules, which reported no unwritable field on any command
# here; this file is hand-maintained from now on.

speclib eda_synopsys 1.0 {

command analyze {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {analyze -format format ?-library lib? ?-define define_list? ?-work work? file_list}

    hover {
        summary {Analyze HDL source files for syntax and semantic errors.}
        synopsis {analyze -format format ?-library lib? ?-define define_list? ?-work work? file_list}
        source {F5}
    }
}

command characterize {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {characterize ?-constraints? instance_list}

    hover {
        summary {Characterize a subdesign for context-dependent optimization.}
        synopsis {characterize ?-constraints? instance_list}
        source {F5}
    }
}

command check_design {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {check_design ?-summary? ?-no_warnings?}

    hover {
        summary {Check the design for consistency problems.}
        synopsis {check_design ?-summary? ?-no_warnings?}
        source {F5}
    }
}

command check_library {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {check_library ?-mw_library_name phys_library_name_list? ?-logic_library_name logic_library_name_list? ?-cells cell_list?}

    hover {
        summary {Check libraries for consistency issues.}
        synopsis {check_library ?-mw_library_name phys_library_name_list? ?-logic_library_name logic_library_name_list? ?-cells cell_list?}
        source {F5}
    }
}

command clock_opt {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {clock_opt ?-effort high|medium|low?}

    hover {
        summary {Perform clock tree synthesis and optimization.}
        synopsis {clock_opt ?-effort high|medium|low?}
        source {F5}
    }
}

command compile {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {compile ?-map_effort effort? ?-area_effort effort? ?-incremental_mapping? ?-exact_map? ?-no_design_rule? ?-scan?}

    hover {
        summary {Compile (synthesize) the current design.}
        synopsis {compile ?-map_effort effort? ?-area_effort effort? ?-incremental_mapping? ?-exact_map? ?-no_design_r}
        source {F5}
    }
}

command compile_ultra {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {compile_ultra ?-incremental? ?-retime? ?-scan? ?-no_autoungroup? ?-no_boundary_optimization? ?-gate_clock? ?-timing_high_effort_script?}

    hover {
        summary {Compile with advanced optimizations.}
        synopsis {compile_ultra ?-incremental? ?-retime? ?-scan? ?-no_autoungroup? ?-no_boundary_optimization? ?-gate_}
        source {F5}
    }
}

command connect_net {
    dialects tcl8.6
    arity 2..
    required_package synopsys-dc

    form Default {connect_net net_name port_pin_list}

    hover {
        summary {Connect a net to pins/ports.}
        synopsis {connect_net net_name port_pin_list}
        source {F5}
    }
}

command create_cell {
    dialects tcl8.6
    arity 2
    required_package synopsys-dc

    form Default {create_cell cell_name lib_cell}

    hover {
        summary {Create a new cell instance.}
        synopsis {create_cell cell_name lib_cell}
        source {F5}
    }
}

command create_floorplan {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {create_floorplan ?-core_utilization util? ?-core_aspect_ratio ratio? ?-left_io2core dist? ?-bottom_io2core dist? ?-right_io2core dist? ?-top_io2core dist?}

    hover {
        summary {Create an initial floorplan.}
        synopsis {create_floorplan ?-core_utilization util? ?-core_aspect_ratio ratio? ?-left_io2core dist? ?-bottom_i}
        source {F5}
    }
}

command create_net {
    dialects tcl8.6
    arity 1
    required_package synopsys-dc

    form Default {create_net net_name}

    hover {
        summary {Create a new net.}
        synopsis {create_net net_name}
        source {F5}
    }
}

command create_port {
    dialects tcl8.6
    arity 1..
    required_package synopsys-dc

    form Default {create_port port_list ?-direction in|out|inout?}

    hover {
        summary {Create a new port in the design.}
        synopsis {create_port port_list ?-direction in|out|inout?}
        source {F5}
    }
}

command current_instance {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {current_instance ?instance_name?}

    hover {
        summary {Navigate into a hierarchical instance.}
        synopsis {current_instance ?instance_name?}
        source {F5}
    }
}

command disconnect_net {
    dialects tcl8.6
    arity 2..
    required_package synopsys-dc

    form Default {disconnect_net net_name object_list|-all}

    hover {
        summary {Disconnect a net from pins/ports.}
        synopsis {disconnect_net net_name object_list|-all}
        source {F5}
    }
}

command elaborate {
    dialects tcl8.6
    arity 1..
    required_package synopsys-dc

    form Default {elaborate design_name ?-library lib? ?-architecture arch? ?-parameters params? ?-update?}

    hover {
        summary {Elaborate a design from analyzed HDL.}
        synopsis {elaborate design_name ?-library lib? ?-architecture arch? ?-parameters params? ?-update?}
        source {F5}
    }
}

command get_timing_paths {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {get_timing_paths ?-from from_list? ?-through through_list? ?-to to_list? ?-delay_type max|min|min_max|max_rise|max_fall|min_rise|min_fall? ?-max_paths n? ?-nworst n? ?-slack_lesser_than value?}

    hover {
        summary {Get timing paths as a collection.}
        synopsis {get_timing_paths ?-from from_list? ?-through through_list? ?-to to_list? ?-delay_type max|min|min_max|max_rise|max_fall|min_rise|min_fall? ?-max_paths n? ?-nworst n? ?-slack_lesser_than value?}
        source {F5}
    }
}

command group {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {group ?-design_name name? ?-cell_name name? cell_list}

    hover {
        summary {Group logic into a hierarchical block.}
        synopsis {group ?-design_name name? ?-cell_name name? cell_list}
        source {F5}
    }
}

command initialize_floorplan {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {initialize_floorplan ?-core_utilization util? ?-core_offset offset?}

    hover {
        summary {Initialize the floorplan from constraints.}
        synopsis {initialize_floorplan ?-core_utilization util? ?-core_offset offset?}
        source {F5}
    }
}

command insert_clock_gating {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {insert_clock_gating ?-regular_only? ?-global? ?-no_hier?}

    hover {
        summary {Insert clock gating logic.}
        synopsis {insert_clock_gating ?-regular_only? ?-global? ?-no_hier?}
        source {F5}
    }
}

command insert_dft {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {insert_dft}

    hover {
        summary {Insert DFT structures (scan chains).}
        synopsis {insert_dft}
        source {F5}
    }
}

command link {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {link ?-remove_sub_design?}

    hover {
        summary {Link the current design to library cells.}
        synopsis {link ?-remove_sub_design?}
        source {F5}
    }
}

command list_designs {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {list_designs ?design_list? ?-show_file?}

    hover {
        summary {List the designs currently loaded in memory.}
        synopsis {list_designs ?design_list? ?-show_file?}
        source {Synopsys DC syn_commands manual}
    }
}

command match {
    dialects tcl8.6
    required_package synopsys-fm

    form Default {match}

    hover {
        summary {Match compare points between reference and implementation.}
        synopsis {match}
        source {F5}
    }
}

command optimize_netlist {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {optimize_netlist -area}

    hover {
        summary {Perform incremental netlist optimization.}
        synopsis {optimize_netlist -area}
        source {F5}
    }
}

command place_opt {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {place_opt ?-effort high|medium|low?}

    hover {
        summary {Perform placement optimization.}
        synopsis {place_opt ?-effort high|medium|low?}
        source {F5}
    }
}

command printvar {
    dialects tcl8.6
    required_package synopsys

    form Default {printvar ?pattern? ?-user_defined? ?-application?}

    hover {
        summary {Print the value of an application variable.}
        synopsis {printvar ?pattern? ?-user_defined? ?-application?}
        source {F5}
    }
}

command read_db {
    dialects tcl8.6
    arity 1..
    required_package synopsys-dc

    form Default {read_db file_names}

    hover {
        summary {Read a .db technology library.}
        synopsis {read_db file_names}
        source {F5}
    }
}

command read_ddc {
    dialects tcl8.6
    arity 1..
    required_package synopsys-dc

    form Default {read_ddc file_names ?-scenarios scenario_list? ?-active_scenarios active_scenario_list?}

    hover {
        summary {Read a Synopsys DDC database.}
        synopsis {read_ddc file_names ?-scenarios scenario_list? ?-active_scenarios active_scenario_list?}
        source {F5}
    }
}

command read_def {
    dialects tcl8.6
    arity 1..
    required_package synopsys-icc2

    form Default {read_def ?-add_def_only_objects all? file_name}

    hover {
        summary {Read a DEF file.}
        synopsis {read_def ?-add_def_only_objects all? file_name}
        source {F5}
    }
}

command read_file {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {read_file file_list ?-define macro_names? ?-format format_name? ?-ilm? ?-library library_name? ?-rtl? ?-single_file single_file_name? ?-work library_name? ?-scenarios scenario_list? ?-active_scenarios active_scenario_list? ?-names_file file_list?}

    hover {
        summary {Read a design file into memory.}
        synopsis {read_file file_list ?-define macro_names? ?-format format_name? ?-ilm? ?-library library_name? ?-rtl? ?-single_file single_file_name? ?-work library_name? ?-scenarios scenario_list? ?-active_scenarios active_scenario_list? ?-names_file file_list?}
        source {F5}
    }
}

command read_lef {
    dialects tcl8.6
    arity 1
    required_package synopsys-icc2

    form Default {read_lef file_name}

    hover {
        summary {Read a LEF technology file.}
        synopsis {read_lef file_name}
        source {F5}
    }
}

command read_sdc {
    dialects tcl8.6
    arity 1..
    required_package synopsys

    form Default {read_sdc file_name ?-echo? ?-syntax_only? ?-version sdc_version?}

    hover {
        summary {Read SDC constraint file.}
        synopsis {read_sdc file_name ?-echo? ?-syntax_only? ?-version sdc_version?}
        source {F5}
    }
}

command read_verilog {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {read_verilog ?-netlist|-rtl? verilog_files}

    hover {
        summary {Read Verilog source files.}
        synopsis {read_verilog ?-netlist|-rtl? verilog_files}
        source {F5}
    }
}

command read_vhdl {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {read_vhdl ?-netlist? file_names}

    hover {
        summary {Read VHDL source files.}
        synopsis {read_vhdl ?-netlist? file_names}
        source {F5}
    }
}

command remove_cell {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {remove_cell cell_list|-all}

    hover {
        summary {Remove a cell from the design.}
        synopsis {remove_cell cell_list|-all}
        source {F5}
    }
}

command remove_design {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {remove_design ?-all? ?-designs? ?design_list?}

    hover {
        summary {Remove a design from memory.}
        synopsis {remove_design ?-all? ?-designs? ?design_list?}
        source {F5}
    }
}

command report_analysis_coverage {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {report_analysis_coverage ?-status_details status_list? ?-check_type check_type_list? ?-exclude_untested? ?-sort_by sort_field? ?-significant_digits digits? ?-nosplit? ?-pre_commands command_list? ?-post_commands command_list?}

    hover {
        summary {Report timing analysis coverage.}
        synopsis {report_analysis_coverage ?-status_details status_list? ?-check_type check_type_list? ?-exclude_untested? ?-sort_by sort_field? ?-significant_digits digits? ?-nosplit? ?-pre_commands command_list? ?-post_commands command_list?}
        source {F5}
    }
}

command report_bottleneck {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {report_bottleneck ?-cost_type cost_type? ?-delay_type delay_type? ?-slack_lesser_than slack_limit? ?-from from_list|-rise_from from_list|-fall_from from_list? ?-to to_list|-rise_to to_list|-fall_to to_list? ?-through through_list? ?-rise_through through_list? ?-fall_through through_list? ?-max_cells cell_count? ?-max_paths path_count? ?-nworst_paths paths_per_endpoint? ?-group group_name? ?-significant_digits digits? ?-nosplit?}

    hover {
        summary {Report timing bottleneck analysis.}
        synopsis {report_bottleneck ?-cost_type cost_type? ?-delay_type delay_type? ?-slack_lesser_than slack_limit? ?-from from_list|-rise_from from_list|-fall_from from_list? ?-to to_list|-rise_to to_list|-fall_to to_list? ?-through through_list? ?-rise_through through_list? ?-fall_through through_list? ?-max_cells cell_count? ?-max_paths path_count? ?-nworst_paths paths_per_endpoint? ?-group group_name? ?-significant_digits digits? ?-nosplit?}
        source {F5}
    }
}

command report_cell {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_cell ?-connections? ?-verbose? ?-physical? ?-only_physical? ?-nosplit? ?-significant_digits digits? ?cell_list?}

    hover {
        summary {Report cell-level information.}
        synopsis {report_cell ?-connections? ?-verbose? ?-physical? ?-only_physical? ?-nosplit? ?-significant_digits digits? ?cell_list?}
        source {F5}
    }
}

command report_clock_gating {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_clock_gating ?-no_hier? ?-verbose? ?-gated? ?-ungated? ?-gating_elements? ?-only cell_list? ?-nosplit? ?-physical? ?-multi_stage? ?-style? ?-structure? ?-scenario scenario_list?}

    hover {
        summary {Report clock gating statistics.}
        synopsis {report_clock_gating ?-no_hier? ?-verbose? ?-gated? ?-ungated? ?-gating_elements? ?-only cell_list? ?-nosplit? ?-physical? ?-multi_stage? ?-style? ?-structure? ?-scenario scenario_list?}
        source {F5}
    }
}

command report_congestion {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_congestion}

    hover {
        summary {Report routing congestion.}
        synopsis {report_congestion}
        source {F5}
    }
}

command report_delay_calculation {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {report_delay_calculation ?-min|-max? ?-from_rise_transition value? ?-from_fall_transition value? -from from_pin -to to_pin|-of_objects objects ?-nosplit? ?-thresholds? ?-crosstalk?}

    hover {
        summary {Report delay calculation details.}
        synopsis {report_delay_calculation ?-min|-max? ?-from_rise_transition value? ?-from_fall_transition value? -from from_pin -to to_pin|-of_objects objects ?-nosplit? ?-thresholds? ?-crosstalk?}
        source {F5}
    }
}

command report_design {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_design ?-nosplit? ?-verbose?}

    hover {
        summary {Report design summary.}
        synopsis {report_design ?-nosplit? ?-verbose?}
        source {F5}
    }
}

command report_hierarchy {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_hierarchy ?-nosplit? ?-full? ?-noleaf?}

    hover {
        summary {Report design hierarchy.}
        synopsis {report_hierarchy ?-nosplit? ?-full? ?-noleaf?}
        source {F5}
    }
}

command report_net {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_net ?-connections? ?-verbose? ?-noflat? ?-transition_times? ?-only_physical? ?-cell_degradation? ?-min? ?-physical? ?-nosplit? ?-significant_digits digits? ?-max_toggle_rate? ?-scenario scenario_list? ?net_list?}

    hover {
        summary {Report net information.}
        synopsis {report_net ?-connections? ?-verbose? ?-noflat? ?-transition_times? ?-only_physical? ?-cell_degradation? ?-min? ?-physical? ?-nosplit? ?-significant_digits digits? ?-max_toggle_rate? ?-scenario scenario_list? ?net_list?}
        source {F5}
    }
}

command report_qor {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_qor ?-nosplit? ?-summary?}

    hover {
        summary {Report Quality of Results metrics.}
        synopsis {report_qor ?-nosplit? ?-summary?}
        source {F5}
    }
}

command report_reference {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_reference ?-nosplit? ?-hierarchy?}

    hover {
        summary {Report library cell references.}
        synopsis {report_reference ?-nosplit? ?-hierarchy?}
        source {F5}
    }
}

command report_status {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {report_status ?-verbose?}

    hover {
        summary {Report verification status.}
        synopsis {report_status ?-verbose?}
        source {F5}
    }
}

command route_auto {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {route_auto ?-max_detail_route_iterations n?}

    hover {
        summary {Perform automatic routing.}
        synopsis {route_auto ?-max_detail_route_iterations n?}
        source {F5}
    }
}

command route_opt {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {route_opt ?-effort high|medium|low?}

    hover {
        summary {Perform post-route optimization.}
        synopsis {route_opt ?-effort high|medium|low?}
        source {F5}
    }
}

command set_app_var {
    dialects tcl8.6
    arity 2
    required_package synopsys

    form Default {set_app_var variable_name value}

    hover {
        summary {Set a Synopsys application variable.}
        synopsis {set_app_var variable_name value}
        source {F5}
    }
}

command set_clock_gating_style {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {set_clock_gating_style ?-sequential_cell none|latch? ?-minimum_bitwidth minsize_value? ?-setup setup_value? ?-hold hold_value? ?-positive_edge_logic logic_spec? ?-negative_edge_logic logic_spec? ?-control_point none|before|after? ?-control_signal scan_enable|test_mode? ?-observation_point true|false? ?-observation_logic_depth depth_value? ?-max_fanout max_fanout_count? ?-num_stages num_stages_count? ?-no_sharing? ?-gicg_pos_cell cell_name? ?-gicg_neg_cell cell_name?}

    hover {
        summary {Specify clock gating implementation style.}
        synopsis {set_clock_gating_style ?-sequential_cell none|latch? ?-minimum_bitwidth minsize_value? ?-setup setup_value? ?-hold hold_value? ?-positive_edge_logic logic_spec? ?-negative_edge_logic logic_spec? ?-control_point none|before|after? ?-control_signal scan_enable|test_mode? ?-observation_point true|false? ?-observation_logic_depth depth_value? ?-max_fanout max_fanout_count? ?-num_stages num_stages_count? ?-no_sharing? ?-gicg_pos_cell cell_name? ?-gicg_neg_cell cell_name?}
        source {F5}
    }
}

command set_host_options {
    dialects tcl8.6
    required_package synopsys

    form Default {set_host_options ?-max_cores n?}

    hover {
        summary {Configure multi-core and host settings.}
        synopsis {set_host_options ?-max_cores n?}
        source {F5}
    }
}

command set_implementation_design {
    dialects tcl8.6
    arity 1
    required_package synopsys-fm

    form Default {set_implementation_design design_name}

    hover {
        summary {Set the implementation design for formal verification.}
        synopsis {set_implementation_design design_name}
        source {F5}
    }
}

command set_reference_design {
    dialects tcl8.6
    arity 1
    required_package synopsys-fm

    form Default {set_reference_design design_name}

    hover {
        summary {Set the reference design for formal verification.}
        synopsis {set_reference_design design_name}
        source {F5}
    }
}

command set_scan_configuration {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {set_scan_configuration ?-add_lockup true|false? ?-chain_count integer|-max_length integer|-exact_length integer|-count_per_domain integer? ?-clock_mixing no_mix|mix_edges|mix_clocks|mix_clocks_not_edges? ?-create_dedicated_scan_out_ports true|false? ?-internal_clocks single|none|multi? ?-insert_terminal_lockup true|false? ?-lockup_type latch|flip_flop? ?-mix_internal_clock_driver true|false? ?-partition vertical|horizontal? ?-style multiplexed_flip_flop|clocked_scan|lssd|aux_clock_lssd|combinational|none?}

    hover {
        summary {Configure scan chain parameters.}
        synopsis {set_scan_configuration ?-add_lockup true|false? ?-chain_count integer|-max_length integer|-exact_length integer|-count_per_domain integer? ?-clock_mixing no_mix|mix_edges|mix_clocks|mix_clocks_not_edges? ?-create_dedicated_scan_out_ports true|false? ?-internal_clocks single|none|multi? ?-insert_terminal_lockup true|false? ?-lockup_type latch|flip_flop? ?-mix_internal_clock_driver true|false? ?-partition vertical|horizontal? ?-style multiplexed_flip_flop|clocked_scan|lssd|aux_clock_lssd|combinational|none?}
        source {F5}
    }
}

command set_technology {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {set_technology ?technology?}

    hover {
        summary {Set the target technology.}
        synopsis {set_technology ?technology?}
        source {F5}
    }
}

command size_cell {
    dialects tcl8.6
    arity 2
    required_package synopsys-dc

    form Default {size_cell cell_name lib_cell}

    hover {
        summary {Resize a cell to a different library cell.}
        synopsis {size_cell cell_name lib_cell}
        source {F5}
    }
}

command swap_cell {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {swap_cell cell_list swap_in ?-dont_preserve_constraints? ?-file file_name? ?-format db|Verilog|EDIF|VHDL?}

    hover {
        summary {Swap a cell with a different library cell.}
        synopsis {swap_cell cell_list swap_in ?-dont_preserve_constraints? ?-file file_name? ?-format db|Verilog|EDIF|VHDL?}
        source {F5}
    }
}

command ungroup {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {ungroup ?-all? ?-flatten? ?-start_level n? ?-simple_names? ?cells?}

    hover {
        summary {Dissolve hierarchy of specified cells.}
        synopsis {ungroup ?-all? ?-flatten? ?-start_level n? ?-simple_names? ?cells?}
        source {F5}
    }
}

command uniquify {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {uniquify ?-force? ?-dont_skip_empty_designs?}

    hover {
        summary {Make each instance of a subdesign unique.}
        synopsis {uniquify ?-force? ?-dont_skip_empty_designs?}
        source {F5}
    }
}

command update_timing {
    dialects tcl8.6
    required_package synopsys-pt

    form Default {update_timing ?-full?}

    hover {
        summary {Update timing in the design.}
        synopsis {update_timing ?-full?}
        source {F5}
    }
}

command verify {
    dialects tcl8.6
    required_package synopsys-fm

    form Default {verify ?-verbose?}

    hover {
        summary {Run formal equivalence checking.}
        synopsis {verify ?-verbose?}
        source {F5}
    }
}

command write {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {write ?-format ddc|verilog|svsim|vhdl? ?-hierarchy? ?-no_implicit? ?-modified? ?-output output_file_name? ?-names_file name_mapping_files? ?-donot_expand_dw? ?-scenarios scenario_list? ?design_list? ?-library library_name?}

    hover {
        summary {Write design data to a file.}
        synopsis {write ?-format ddc|verilog|svsim|vhdl? ?-hierarchy? ?-no_implicit? ?-modified? ?-output output_file_name? ?-names_file name_mapping_files? ?-donot_expand_dw? ?-scenarios scenario_list? ?design_list? ?-library library_name?}
        source {F5}
    }
}

command write_def {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {write_def ?-version version? file_name}

    hover {
        summary {Write a DEF file.}
        synopsis {write_def ?-version version? file_name}
        source {F5}
    }
}

command write_file {
    dialects tcl8.6
    required_package synopsys-dc

    form Default {write_file ?-format ddc|verilog|svsim|vhdl? ?-hierarchy? ?-no_implicit? ?-modified? ?-output output_file_name? ?-names_file name_mapping_files? ?-donot_expand_dw? ?-scenarios scenario_list? ?design_list? ?-library library_name?}

    hover {
        summary {Write design to file in specified format.}
        synopsis {write_file ?-format ddc|verilog|svsim|vhdl? ?-hierarchy? ?-no_implicit? ?-modified? ?-output output_file_name? ?-names_file name_mapping_files? ?-donot_expand_dw? ?-scenarios scenario_list? ?design_list? ?-library library_name?}
        source {F5}
    }
}

command write_gds {
    dialects tcl8.6
    required_package synopsys-icc2

    form Default {write_gds ?-long_names? ?-output file_name?}

    hover {
        summary {Write a GDSII stream file.}
        synopsis {write_gds ?-long_names? ?-output file_name?}
        source {F5}
    }
}

command write_sdc {
    dialects tcl8.6
    required_package synopsys

    form Default {write_sdc file_name ?-version sdc_version? ?-compress compression? ?-include categories_list? ?-nosplit?}

    hover {
        summary {Write SDC constraints to a file.}
        synopsis {write_sdc file_name ?-version sdc_version? ?-compress compression? ?-include categories_list? ?-nosplit?}
        source {F5}
    }
}

}
