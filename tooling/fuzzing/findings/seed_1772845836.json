{
  "seed": 1772845836,
  "mismatches": [
    {
      "kind": "crash",
      "description": "both vm and vm_opt crashed",
      "backend_a": "vm",
      "backend_b": "vm_opt",
      "detail_a": "TIMEOUT",
      "detail_b": "TIMEOUT"
    },
    {
      "kind": "crash",
      "description": "vm crashed but tclsh(tclsh9.0) did not",
      "backend_a": "vm",
      "backend_b": "tclsh(tclsh9.0)",
      "detail_a": "TIMEOUT",
      "detail_b": "can't read \"y\": no such variable\n    while executing\n\"if {($y ? (68 >> $y) : (48 == -4.93))} {\n    set y [expr {((-69.03 ? wide(-29.57) : (42 ? -23 : $y)) << $y)}]\n    for {set flag 0} {$flag < 2} {incr f...\"\n    (file \"/var/folders/2s/clyp06qs0_d8rrr2zqtb4qj00000gn/T/tmpgwdilvnc.tcl\" line 6)"
    },
    {
      "kind": "crash",
      "description": "vm_opt crashed but tclsh(tclsh9.0) did not",
      "backend_a": "vm_opt",
      "backend_b": "tclsh(tclsh9.0)",
      "detail_a": "TIMEOUT",
      "detail_b": "can't read \"y\": no such variable\n    while executing\n\"if {($y ? (68 >> $y) : (48 == -4.93))} {\n    set y [expr {((-69.03 ? wide(-29.57) : (42 ? -23 : $y)) << $y)}]\n    for {set flag 0} {$flag < 2} {incr f...\"\n    (file \"/var/folders/2s/clyp06qs0_d8rrr2zqtb4qj00000gn/T/tmpgwdilvnc.tcl\" line 6)"
    }
  ],
  "fixed": true,
  "fix": "VM now correctly errors on missing variable",
  "category": "vm-timeout-resolved"
}
