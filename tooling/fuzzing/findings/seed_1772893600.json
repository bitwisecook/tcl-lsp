{
  "seed": 1772893600,
  "mismatches": [
    {
      "kind": "crash",
      "description": "vm_opt crashed but vm did not",
      "backend_a": "vm",
      "backend_b": "vm_opt",
      "detail_a": "invalid command name \"a-z\"",
      "detail_b": "TIMEOUT"
    },
    {
      "kind": "crash",
      "description": "vm_opt crashed but tclsh(tclsh9.0) did not",
      "backend_a": "vm_opt",
      "backend_b": "tclsh(tclsh9.0)",
      "detail_a": "TIMEOUT",
      "detail_b": "invalid command name \"a-z\"\n    while executing\n\"a-z\"\n    (\"while\" body line 17)\n    invoked from within\n\"while {$y < 5} {\n    lassign {yes} y\n    for {set y 0} {$y < 6} {incr y} {\n        if {$y < -1} {\n            expr {91}\n            expr {(((-88.50 ||...\"\n    (file \"/var/folders/2s/clyp06qs0_d8rrr2zqtb4qj00000gn/T/tmpagaoicmp.tcl\" line 2)"
    }
  ],
  "category": "vm-timeout-invalid-cmd",
  "triage": "VM loops on script with invalid commands; tclsh errors immediately",
  "fixed": true,
  "fix": "resolved by prior VM fixes (vm-timeout-invalid-cmd)"
}
