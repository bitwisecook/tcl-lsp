# Generated by the benchmark harness; reconstitutible from MANIFEST.toml.
corpus/
servers/
.stage/
.build/
ci-results/
ci-graphs/
# CI's render input: the committed results plus this run's measurement, merged
# so neither source directory is clobbered (see .github/workflows/perf.yml).
render/
