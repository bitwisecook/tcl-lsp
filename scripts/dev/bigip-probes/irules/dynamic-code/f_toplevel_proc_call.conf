# Top-level proc via call. Expected: LOADED, r=x — the only compile-visible route.
ltm rule __tcl_lsp_probe_dyn_f_toplevel_proc_call {
proc dp6 {} { return x }
when RULE_INIT {
  log local0. "DYN f_toplevel_proc_call r=[call dp6]"
}
}
