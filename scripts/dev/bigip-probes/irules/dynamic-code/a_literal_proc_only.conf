# proc written literally inside a when body.
# Expected: REJECT  error: [command is not valid in the current scope]
ltm rule __tcl_lsp_probe_dyn_a_literal_proc_only {
when RULE_INIT {
  proc dp1 {} { return x }
  log local0. "DYN a_literal_proc_only defined"
}
}
