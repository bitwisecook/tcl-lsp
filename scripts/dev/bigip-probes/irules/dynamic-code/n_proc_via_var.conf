ltm rule __tcl_lsp_probe_dyn_n_proc_via_var {
when RULE_INIT {
  set s "proc dpn \{\} \{ return n \}"
  set rc [catch {uplevel #0 $s} e]
  set r [catch {uplevel #0 dpn} v]
  log local0. "DYN n_proc_via_var define_rc=$rc call_rc=$r v=$v"
}
}
