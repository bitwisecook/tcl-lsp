# Uncaught runtime error: runs to the failing command, logs 01220001 at err
# level, aborts the handler, and resets the client connection.
ltm rule lab_err {
when HTTP_REQUEST {
  log local0. "ERRTEST before"
  set x [expr {1/0}]
  log local0. "ERRTEST after"
}
}
