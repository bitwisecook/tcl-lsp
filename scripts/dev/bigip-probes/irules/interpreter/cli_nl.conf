cli script probe_cli_nl {
proc script::run {} {
  set n 1
  while {$n < 30}
  {
    set n [expr {$n + 30}]
  }
  puts "CLI while_nextline n=$n"
  set out "NOTRUN"
  if {0} {
    set out "then"
  }
  else {
    set out "else-branch"
  }
  puts "CLI else_nextline out=$out"
}
}
