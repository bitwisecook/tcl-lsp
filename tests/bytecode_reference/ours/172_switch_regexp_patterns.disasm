ByteCode ::top, 6 instructions, 11 bytes, 4 literals, 0 variables
  Literals:
    0: "proc"
    1: "classify"
    2: "s"
    3: "\n    switch -regexp $s {\n        {^..."
  Instructions:
  # entry_1:
    (0) push1 0	# "proc"
    (2) push1 1	# "classify"
    (4) push1 2	# "s"
    (6) push1 3	# "\n    switch -regexp $s {\n        {^..."
    (8) invokeStk1 4	# proc
  # exit_2:
    (10) done

ByteCode ::classify, 6 instructions, 11 bytes, 3 literals, 1 variables
  Literals:
    0: "switch"
    1: "-regexp"
    2: "\n        {^\d+$}    { return \"numbe..."
  Local variables:
    %v0: "s"
  Instructions:
  # entry_1:
    (0) push1 0	# "switch"
    (2) push1 1	# "-regexp"
    (4) loadScalar1 %v0	# var "s"
    (6) push1 2	# "\n        {^\d+$}    { return \"numbe..."
    (8) invokeStk1 4	# switch
  # exit_2:
    (10) done
