catch {
    expr {(!(-30.04 ? 45 : (40 || 82)))}
    set data 0
    while {$data < 2} {
        regexp "\\d+" "34 jrgup ."
        set z eatjjn
        set data -705
        incr data
    }
} tmp
set flag no
