# string.test category triage — Stream 2 (tcl9-arith-wording).
# 375/705 passing; 7 skipped; 323 failing.  This stream only triaged
# the clusters whose root cause it actually observed; the remainder
# defaults to must_pass and a follow-up stream is expected to walk
# the residual failures and bin them.
#
# Sampled clusters (root causes — apply to many test IDs each):
#
#   * `string compare` / `string equal` with `-nocase` / `-length`
#     flags (~50 tests): MUST_PASS — semantic feature.  Helpers
#     `string_compare_full` / `string_equal_full` are wired into
#     `tcl_string.zig` but the dispatch glue in `cmds/string.zig`
#     was reverted in this stream's commit because it broke
#     tcltest's own equality-probe path.  Re-attempt with the
#     dispatcher debugged.
#
#   * `string is dict` (~17 tests, IDs 32.1 / 32.2 / … / 32.17):
#     GOOD_TO_HAVE — Tcl 9 subcommand class our `string is`
#     dispatch doesn't know yet.
#
#   * Subcommand prefix abbreviation (`string co` → `string compare`,
#     a handful of tests): MUST_PASS — real Tcl feature
#     (`Tcl_GetIndexFromObj`).
#
#   * `bytearray` identity tests (string-2.38a / 2.38c / 2.38d /
#     2.38e, 3.45a / 3.45d): JUST_TO_MATCH_CTCL — internal-rep
#     identity, not user-visible behaviour.
#
# Per-test entries follow as the next stream walks them.  No entries
# yet means the residual 323 failures default to must_pass, which is
# correct for the gate.

[meta]
bundle = "string.test"
total = 705
stream = "tcl9-arith-wording"

# (No entries yet — see header for sampled-cluster hand-off notes.)
