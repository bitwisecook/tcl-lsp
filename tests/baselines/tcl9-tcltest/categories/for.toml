# for.test categorisation (Tcl 9.0.3 source tree).
# Bundle now reaches its tcltest summary line — the preserveCore trap
# was resolved by the trace + var-resolution fixes.  Residual must_pass
# failures triaged below.

good_to_have = [
  "3.6",    # for-3.6: edge case in for-loop body that mutates the
            # loop variable from inside the body — the for next-clause
            # re-evaluation doesn't propagate the mutation back to
            # the variable our condition reads.
]
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 1
just_to_match_ctcl_failing = 0
skip_failing = 0
