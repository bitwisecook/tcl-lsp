# compExpr-old.test categorisation (Tcl 9.0.3 source tree).
# Stream: tcl9-arith-wording.  See ``categories/README.md`` for
# bucket definitions.  Unlisted IDs default to ``must_pass`` —
# anything new from upstream fails closed.
#
# Snapshot: 179/184 passing; 2 skipped (ieeeFloatingPoint constraint);
# 3 categorised below.  Was timeout-out before the binary / octal
# literal panic fix landed.

# Real semantic feature gaps tracked but non-gating.
good_to_have = [
  "3.7",          # compExpr-old-3.7: ``hello_world`` proc — the
                  #   ``hello_world`` and ``12days`` stress-test
                  #   procs in the test source are stubbed out by
                  #   ``_patch_hello_world_procs`` because their
                  #   deeply nested ``[expr {a?b:c?d:e?...:[set v
                  #   ...]}]`` ternaries trip the AOT codegen's
                  #   short-circuit-with-side-effect handling.
                  #   Re-enable once the ternary-side-effect codegen
                  #   bug lands.
  "3.8",          # compExpr-old-3.8: ``do_twelve_days`` — same
                  #   stub-driven failure as 3.7.
  "14.13",        # compExpr-old-14.13: array-element ref in unbraced
                  #   ``expr $a(123)``.  The runtime eval-script path
                  #   doesn't yet wire array-elem subst through the
                  #   expr arg; AOT path handles it correctly.
]

just_to_match_ctcl = []

# No bytecode-internal probes in this stem.
skip = []

[baseline]
# Snapshot at triage time.  good_to_have_failing must not grow.
good_to_have_failing = 3
just_to_match_ctcl_failing = 0
skip_failing = 0
