# incr.test category triage — Stream 2 (tcl9-arith-wording).
# 68/69 passing; 1 listed below.  Default for unlisted failures is must_pass.

[meta]
bundle = "incr.test"
total = 69
stream = "tcl9-arith-wording"

[[entries]]
id = "1.32"
bucket = "must_pass"
reason = "incr i 0xFFFFFFFFFFFFFF80 (and 11 similar large-hex deltas) — should add the bignum value to i, but our tcl_incr falls into the i64 path because obj_type of a hex-shaped string is TYPE_STRING (not TYPE_BIGNUM). obj_get_int truncates 18446744073709551488 to -128. Real precision bug in incr's bignum-trigger probe; tcl_incr should call is_bignum (parse-aware) instead of obj_type."
