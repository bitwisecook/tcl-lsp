# unixInit.test — auto-generated WASM baseline.
# bucket = 'W8-missing-command'
# reason = 'invalid command name: zipfs'
# classification: deferred-wasi
# rationale: needs the unix init path (zipfs library mount)
trap_allowed = true
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 0
observed_passed = 0
observed_failed = 0
observed_skipped = 0

[crash]
type = "Trap"
msg = "tcl trap: site=99 invalid command name \"zipfs\""
commands = ["zipfs"]