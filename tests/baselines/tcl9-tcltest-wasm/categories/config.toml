# config.test — auto-generated WASM baseline.
# bucket = 'W-mixed-fail'
# reason = '8 of 9 failed'
# classification: deferred-wasi
# rationale: asserts build-time configuration / pkgconfig
trap_allowed = false
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 9
observed_passed = 1
observed_failed = 8
observed_skipped = 0

[failing]
# Test IDs failing today — kept here for visibility, not gating.
ids = ["pkgconfig-1.2", "pkgconfig-1.3", "pkgconfig-2.0", "pkgconfig-2.1", "pkgconfig-2.2", "pkgconfig-2.3", "pkgconfig-2.4", "pkgconfig-2.5"]