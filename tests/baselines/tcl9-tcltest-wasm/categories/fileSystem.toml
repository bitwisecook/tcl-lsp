# fileSystem.test — auto-generated WASM baseline.
# bucket = 'W0-stub-trap'
# reason = 'unsupported command: file'
# classification: deferred-wasi
# rationale: needs a writable filesystem (file delete -force, …)
trap_allowed = true
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 0
observed_passed = 0
observed_failed = 0
observed_skipped = 0

[failing]
# Test IDs failing today — kept here for visibility, not gating.
ids = ["filesystem-1.2", "filesystem-1.3", "filesystem-1.4", "filesystem-1.7", "filesystem-1.11", "filesystem-1.12", "filesystem-1.27", "filesystem-1.29.1"]

[crash]
type = "Trap"
msg = "tcl trap: site=136 unsupported command: file delete -force (recursive)"
commands = ["file"]