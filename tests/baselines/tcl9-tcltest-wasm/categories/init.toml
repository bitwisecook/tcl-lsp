# init.test — auto-generated WASM baseline.
# bucket = 'W8-missing-command'
# reason = 'invalid command name: tcltest::configure'
# classification: deferred-wasi
# rationale: needs the filesystem auto-loader bootstrap
trap_allowed = true
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 0
observed_passed = 0
observed_failed = 0
observed_skipped = 0

[failing]
# Test IDs failing today — kept here for visibility, not gating.
ids = ["init-1.1", "init-1.2", "init-1.3", "init-1.4", "init-1.5", "init-1.6", "init-1.7", "init-1.8"]

[crash]
type = "Trap"
msg = "tcl trap: site=261 invalid command name \"tcltest::configure\""
commands = ["tcltest::configure"]