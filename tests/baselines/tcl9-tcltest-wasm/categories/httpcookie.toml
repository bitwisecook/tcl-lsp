# httpcookie.test — auto-generated WASM baseline.
# bucket = 'W-mixed-fail'
# reason = '6 of 60 failed'
# classification: deferred-wasi
# rationale: HTTP cookie jar — needs the HTTP transport
trap_allowed = false
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 60
observed_passed = 54
observed_failed = 6
observed_skipped = 0

[failing]
# Test IDs failing today — kept here for visibility, not gating.
ids = ["http-cookiejar-1.1", "http-cookiejar-1.2", "http-cookiejar-3.1", "http-cookiejar-3.2", "http-cookiejar-3.3", "http-cookiejar-3.4"]