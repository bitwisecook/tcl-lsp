# exec.test — auto-generated WASM baseline.
# bucket = 'W0-runtime-error'
# reason = 'tcl trap: site=368 permission denied: exec requires CAP_EXEC'
# classification: deferred-wasi
# rationale: needs fork/exec — blocked by CAP_EXEC under WASI
trap_allowed = true
good_to_have = []
just_to_match_ctcl = []
skip = []

[baseline]
good_to_have_failing = 0
just_to_match_ctcl_failing = 0
skip_failing = 0
observed_total = 0
observed_passed = 0
observed_failed = 0
observed_skipped = 0

[failing]
# Test IDs failing today — kept here for visibility, not gating.
ids = ["exec-1.1", "exec-1.4", "exec-2.5", "exec-3.1", "exec-3.4", "exec-3.5", "exec-3.6", "exec-3.7", "exec-4.1", "exec-4.2", "exec-4.3", "exec-4.4", "exec-4.5"]

[crash]
type = "Trap"
msg = "tcl trap: site=368 permission denied: exec requires CAP_EXEC"