//! Placeholder for the `value2` builtin batch — filled in by the category
//! port. Registers nothing until implemented.

use crate::builtins::BuiltinSpec;

pub(super) fn registrations() -> Vec<(&'static str, BuiltinSpec)> {
    vec![]
}
