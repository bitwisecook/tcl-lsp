// @generated by scripts/codegen/gen_bigip_model_rust.py — do not edit.
//! Generated BIG-IP `pem` model structs.

#![allow(clippy::struct_excessive_bools)]
#![allow(clippy::derivable_impls)]
#![allow(clippy::default_trait_access)]
#![allow(unused_imports)]

use super::*;

/// Mirrors Python `BigipPemForwardingEndpoint`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemForwardingEndpoint {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `pool`
    pub pool: String,
    /// `snat_pool`
    pub snat_pool: String,
    /// `source_ip`
    pub source_ip: String,
    /// `destination_ip`
    pub destination_ip: String,
    /// `type_`
    pub type_: String,
    /// `persistence`
    pub persistence: String,
    /// `translate_address`
    pub translate_address: String,
    /// `translate_service`
    pub translate_service: String,
    /// `fallback`
    pub fallback: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemForwardingEndpoint {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            pool: String::new(),
            snat_pool: String::new(),
            source_ip: String::new(),
            destination_ip: String::new(),
            type_: String::new(),
            persistence: String::new(),
            translate_address: String::new(),
            translate_service: String::new(),
            fallback: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemInterceptionEndpoint`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemInterceptionEndpoint {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `pool`
    pub pool: String,
    /// `persistence`
    pub persistence: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemInterceptionEndpoint {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            pool: String::new(),
            persistence: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemListener`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemListener {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `profile_spm`
    pub profile_spm: String,
    /// `profile_subscriber_mgmt`
    pub profile_subscriber_mgmt: String,
    /// `virtual_servers`
    pub virtual_servers: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemListener {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            profile_spm: String::new(),
            profile_subscriber_mgmt: String::new(),
            virtual_servers: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `profile_type`
    pub profile_type: String,
    /// `defaults_from`
    pub defaults_from: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            profile_type: String::new(),
            defaults_from: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemRatingGroup`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemRatingGroup {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rating_group_id`
    pub rating_group_id: String,
    /// `default_quota`
    pub default_quota: String,
    /// `default_quota_holding_time`
    pub default_quota_holding_time: String,
    /// `default_validity_time`
    pub default_validity_time: String,
    /// `default_threshold`
    pub default_threshold: String,
    /// `total_octets`
    pub total_octets: String,
    /// `input_octets`
    pub input_octets: String,
    /// `output_octets`
    pub output_octets: String,
    /// `time`
    pub time: String,
    /// `consumption_time`
    pub consumption_time: String,
    /// `usage_time`
    pub usage_time: String,
    /// `volume`
    pub volume: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemRatingGroup {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rating_group_id: String::new(),
            default_quota: String::new(),
            default_quota_holding_time: String::new(),
            default_validity_time: String::new(),
            default_threshold: String::new(),
            total_octets: String::new(),
            input_octets: String::new(),
            output_octets: String::new(),
            time: String::new(),
            consumption_time: String::new(),
            usage_time: String::new(),
            volume: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemRule`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemRule {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `source`
    pub source: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemRule {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            source: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipPemServiceChainEndpoint`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipPemServiceChainEndpoint {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `service_endpoints`
    pub service_endpoints: Vec<String>,
    /// `steering_policy`
    pub steering_policy: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipPemServiceChainEndpoint {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            service_endpoints: Vec::new(),
            steering_policy: String::new(),
            range: None,
        }
    }
}
