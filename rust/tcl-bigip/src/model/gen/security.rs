// @generated by scripts/codegen/gen_bigip_model_rust.py — do not edit.
//! Generated BIG-IP `security` model structs (port of `dialects/f5/bigip/model/_security.py`).

#![allow(clippy::struct_excessive_bools)]
#![allow(clippy::derivable_impls)]
#![allow(clippy::default_trait_access)]
#![allow(unused_imports)]

use super::*;

/// Mirrors Python `BigipSecurityBotDefenseProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityBotDefenseProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `app_service`
    pub app_service: String,
    /// `template`
    pub template: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityBotDefenseProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            app_service: String::new(),
            template: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityDeviceIdAttribute`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityDeviceIdAttribute {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `id_`
    pub id_: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityDeviceIdAttribute {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            id_: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityDosProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityDosProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `app_service`
    pub app_service: String,
    /// `threshold_sensitivity`
    pub threshold_sensitivity: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityDosProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            app_service: String::new(),
            threshold_sensitivity: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallAddressList`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallAddressList {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `addresses`
    pub addresses: Vec<String>,
    /// `address_lists`
    pub address_lists: Vec<String>,
    /// `fqdns`
    pub fqdns: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallAddressList {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            addresses: Vec::new(),
            address_lists: Vec::new(),
            fqdns: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallConfigChangeLog`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallConfigChangeLog {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `log_publisher`
    pub log_publisher: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallConfigChangeLog {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            log_publisher: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallConfigEntityId`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallConfigEntityId {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `entity_id`
    pub entity_id: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallConfigEntityId {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            entity_id: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallGlobalFqdnPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallGlobalFqdnPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `context`
    pub context: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallGlobalFqdnPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            context: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallGlobalRules`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallGlobalRules {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `enforced_policy`
    pub enforced_policy: String,
    /// `staged_policy`
    pub staged_policy: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallGlobalRules {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            enforced_policy: String::new(),
            staged_policy: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallManagementIpRules`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallManagementIpRules {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallManagementIpRules {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallOnDemandCompilation`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallOnDemandCompilation {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallOnDemandCompilation {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallOnDemandRuleDeploy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallOnDemandRuleDeploy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallOnDemandRuleDeploy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `rule_lists`
    pub rule_lists: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            rule_lists: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallPortList`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallPortList {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `ports`
    pub ports: Vec<String>,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallPortList {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            ports: Vec::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallPortMisusePolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallPortMisusePolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `default_log`
    pub default_log: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallPortMisusePolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            default_log: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallRuleList`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallRuleList {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `rule_objects`
    pub rule_objects: Vec<crate::value::FirewallRule>,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallRuleList {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            rules: Vec::new(),
            rule_objects: Vec::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallSchedule`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallSchedule {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `daily_hour_end`
    pub daily_hour_end: String,
    /// `daily_hour_start`
    pub daily_hour_start: String,
    /// `days_of_week`
    pub days_of_week: Vec<String>,
    /// `date_valid_end`
    pub date_valid_end: String,
    /// `date_valid_start`
    pub date_valid_start: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallSchedule {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            daily_hour_end: String::new(),
            daily_hour_start: String::new(),
            days_of_week: Vec::new(),
            date_valid_end: String::new(),
            date_valid_start: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallUserDomain`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallUserDomain {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `domain`
    pub domain: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallUserDomain {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            domain: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallUserList`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallUserList {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `users`
    pub users: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallUserList {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            users: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityFirewallUuidDefaultAutogenerate`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityFirewallUuidDefaultAutogenerate {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `auto_generate_uuid`
    pub auto_generate_uuid: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityFirewallUuidDefaultAutogenerate {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            auto_generate_uuid: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityHttpProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityHttpProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `defaults_from`
    pub defaults_from: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityHttpProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            defaults_from: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityIpIntelligenceFeedList`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityIpIntelligenceFeedList {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `feeds`
    pub feeds: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityIpIntelligenceFeedList {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            feeds: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityIpIntelligenceGlobalPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityIpIntelligenceGlobalPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `log_blacklist_category`
    pub log_blacklist_category: String,
    /// `log_publisher`
    pub log_publisher: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityIpIntelligenceGlobalPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            log_blacklist_category: String::new(),
            log_publisher: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityIpIntelligencePolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityIpIntelligencePolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `default_action`
    pub default_action: String,
    /// `default_log_blacklist_hit_only`
    pub default_log_blacklist_hit_only: String,
    /// `default_log_blacklist_category`
    pub default_log_blacklist_category: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityIpIntelligencePolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            default_action: String::new(),
            default_log_blacklist_hit_only: String::new(),
            default_log_blacklist_category: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityLogProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityLogProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `application_data`
    pub application_data: String,
    /// `network_data`
    pub network_data: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityLogProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            application_data: String::new(),
            network_data: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityNatDestinationTranslation`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityNatDestinationTranslation {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `type_`
    pub type_: String,
    /// `addresses`
    pub addresses: Vec<String>,
    /// `ports`
    pub ports: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityNatDestinationTranslation {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            type_: String::new(),
            addresses: Vec::new(),
            ports: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityNatPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityNatPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `rule_lists`
    pub rule_lists: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityNatPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            rule_lists: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityNatSourceTranslation`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityNatSourceTranslation {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `type_`
    pub type_: String,
    /// `addresses`
    pub addresses: Vec<String>,
    /// `ports`
    pub ports: Vec<String>,
    /// `traffic_group`
    pub traffic_group: String,
    /// `egress_interfaces_disabled`
    pub egress_interfaces_disabled: bool,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityNatSourceTranslation {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            type_: String::new(),
            addresses: Vec::new(),
            ports: Vec::new(),
            traffic_group: String::new(),
            egress_interfaces_disabled: false,
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityPacketFilterDefaultRules`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityPacketFilterDefaultRules {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `action`
    pub action: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityPacketFilterDefaultRules {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            action: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityPacketFilterPolicy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityPacketFilterPolicy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `rules`
    pub rules: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityPacketFilterPolicy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            rules: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityProtectedZone`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityProtectedZone {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `enabled`
    pub enabled: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityProtectedZone {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            enabled: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityProtocolInspectionComplianceMap`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityProtocolInspectionComplianceMap {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `insp_id`
    pub insp_id: String,
    /// `key_type`
    pub key_type: String,
    /// `value_type`
    pub value_type: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityProtocolInspectionComplianceMap {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            insp_id: String::new(),
            key_type: String::new(),
            value_type: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityProtocolInspectionComplianceObject`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityProtocolInspectionComplianceObject {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `insp_id`
    pub insp_id: String,
    /// `type_`
    pub type_: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityProtocolInspectionComplianceObject {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            insp_id: String::new(),
            type_: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecuritySshProfile`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecuritySshProfile {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `defaults_from`
    pub defaults_from: String,
    /// `timeout`
    pub timeout: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecuritySshProfile {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            defaults_from: String::new(),
            timeout: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipSecurityZone`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipSecurityZone {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `vlans`
    pub vlans: Vec<String>,
    /// `tunnels`
    pub tunnels: Vec<String>,
    /// `interfaces`
    pub interfaces: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipSecurityZone {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            vlans: Vec::new(),
            tunnels: Vec::new(),
            interfaces: Vec::new(),
            range: None,
        }
    }
}
