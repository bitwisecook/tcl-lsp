// @generated by scripts/codegen/gen_bigip_model_rust.py — do not edit.
//! Generated BIG-IP `gtm` model structs (port of `dialects/f5/bigip/model/_gtm.py`).

#![allow(clippy::struct_excessive_bools)]
#![allow(clippy::derivable_impls)]
#![allow(clippy::default_trait_access)]
#![allow(unused_imports)]

use super::*;

/// Mirrors Python `BigipGtmDatacenter`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmDatacenter {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `contact`
    pub contact: String,
    /// `location`
    pub location: String,
    /// `description`
    pub description: String,
    /// `prober_pool`
    pub prober_pool: String,
    /// `prober_preference`
    pub prober_preference: String,
    /// `prober_fallback`
    pub prober_fallback: String,
    /// `state`
    pub state: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmDatacenter {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            contact: String::new(),
            location: String::new(),
            description: String::new(),
            prober_pool: String::new(),
            prober_preference: String::new(),
            prober_fallback: String::new(),
            state: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmDistributedApp`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmDistributedApp {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `wide_ips`
    pub wide_ips: Vec<String>,
    /// `persist_cidr`
    pub persist_cidr: String,
    /// `dependency_level`
    pub dependency_level: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmDistributedApp {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            wide_ips: Vec::new(),
            persist_cidr: String::new(),
            dependency_level: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmGlobalSettingsGeneral`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmGlobalSettingsGeneral {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `auto_discovery`
    pub auto_discovery: String,
    /// `synchronization`
    pub synchronization: String,
    /// `synchronization_group_name`
    pub synchronization_group_name: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmGlobalSettingsGeneral {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            auto_discovery: String::new(),
            synchronization: String::new(),
            synchronization_group_name: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmGlobalSettingsLoadBalancing`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmGlobalSettingsLoadBalancing {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `topology_longest_match`
    pub topology_longest_match: String,
    /// `ignore_path_ttl`
    pub ignore_path_ttl: String,
    /// `respect_dependent_objects`
    pub respect_dependent_objects: String,
    /// `verify_vs_availability`
    pub verify_vs_availability: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmGlobalSettingsLoadBalancing {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            topology_longest_match: String::new(),
            ignore_path_ttl: String::new(),
            respect_dependent_objects: String::new(),
            verify_vs_availability: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmGlobalSettingsMetrics`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmGlobalSettingsMetrics {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `metrics_collection_protocols`
    pub metrics_collection_protocols: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmGlobalSettingsMetrics {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            metrics_collection_protocols: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmGlobalSettingsMetricsExclusions`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmGlobalSettingsMetricsExclusions {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `addresses`
    pub addresses: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmGlobalSettingsMetricsExclusions {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            addresses: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmLink`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmLink {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `datacenter`
    pub datacenter: String,
    /// `monitor`
    pub monitor: String,
    /// `prober_pool`
    pub prober_pool: String,
    /// `state`
    pub state: String,
    /// `weight`
    pub weight: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmLink {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            datacenter: String::new(),
            monitor: String::new(),
            prober_pool: String::new(),
            state: String::new(),
            weight: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmListener`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmListener {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `address`
    pub address: String,
    /// `port`
    pub port: String,
    /// `ip_protocol`
    pub ip_protocol: String,
    /// `mask`
    pub mask: String,
    /// `pool`
    pub pool: String,
    /// `profiles`
    pub profiles: Vec<String>,
    /// `rules`
    pub rules: Vec<String>,
    /// `source_address_translation`
    pub source_address_translation: String,
    /// `state`
    pub state: String,
    /// `vlans`
    pub vlans: Vec<String>,
    /// `vlans_disabled`
    pub vlans_disabled: bool,
    /// `vlans_enabled`
    pub vlans_enabled: bool,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmListener {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            address: String::new(),
            port: String::new(),
            ip_protocol: String::new(),
            mask: String::new(),
            pool: String::new(),
            profiles: Vec::new(),
            rules: Vec::new(),
            source_address_translation: String::new(),
            state: String::new(),
            vlans: Vec::new(),
            vlans_disabled: false,
            vlans_enabled: false,
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmListenerDohProxy`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmListenerDohProxy {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `address`
    pub address: String,
    /// `port`
    pub port: String,
    /// `pool`
    pub pool: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmListenerDohProxy {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            address: String::new(),
            port: String::new(),
            pool: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmListenerDohServer`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmListenerDohServer {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `address`
    pub address: String,
    /// `port`
    pub port: String,
    /// `pool`
    pub pool: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmListenerDohServer {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            address: String::new(),
            port: String::new(),
            pool: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmPool`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmPool {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `record_type`
    pub record_type: String,
    /// `members`
    pub members: Vec<BigipGtmPoolMember>,
    /// `monitor`
    pub monitor: String,
    /// `alternate_mode`
    pub alternate_mode: String,
    /// `fallback_mode`
    pub fallback_mode: String,
    /// `load_balancing_mode`
    pub load_balancing_mode: String,
    /// `ttl`
    pub ttl: String,
    /// `description`
    pub description: String,
    /// `state`
    pub state: String,
    /// `verify_member_availability`
    pub verify_member_availability: String,
    /// `fallback_ip`
    pub fallback_ip: String,
    /// `max_answers_returned`
    pub max_answers_returned: String,
    /// `qos_hit_ratio`
    pub qos_hit_ratio: String,
    /// `qos_hops`
    pub qos_hops: String,
    /// `qos_kbps`
    pub qos_kbps: String,
    /// `qos_lcs`
    pub qos_lcs: String,
    /// `qos_packet_rate`
    pub qos_packet_rate: String,
    /// `qos_rtt`
    pub qos_rtt: String,
    /// `qos_topology`
    pub qos_topology: String,
    /// `qos_vs_capacity`
    pub qos_vs_capacity: String,
    /// `qos_vs_score`
    pub qos_vs_score: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmPool {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            record_type: String::new(),
            members: Vec::new(),
            monitor: String::new(),
            alternate_mode: String::new(),
            fallback_mode: String::new(),
            load_balancing_mode: String::new(),
            ttl: String::new(),
            description: String::new(),
            state: String::new(),
            verify_member_availability: String::new(),
            fallback_ip: String::new(),
            max_answers_returned: String::new(),
            qos_hit_ratio: String::new(),
            qos_hops: String::new(),
            qos_kbps: String::new(),
            qos_lcs: String::new(),
            qos_packet_rate: String::new(),
            qos_rtt: String::new(),
            qos_topology: String::new(),
            qos_vs_capacity: String::new(),
            qos_vs_score: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmPoolMember`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmPoolMember {
    /// `name`
    pub name: String,
    /// `description`
    pub description: String,
    /// `state`
    pub state: String,
    /// `member_order`
    pub member_order: String,
    /// `order`
    pub order: String,
    /// `service_port`
    pub service_port: String,
    /// `ratio`
    pub ratio: String,
    /// `monitor`
    pub monitor: String,
    /// `depends_on`
    pub depends_on: String,
    /// `limit_max_bps`
    pub limit_max_bps: String,
    /// `limit_max_connections`
    pub limit_max_connections: String,
    /// `limit_max_pps`
    pub limit_max_pps: String,
    /// `static_target`
    pub static_target: String,
}

impl Default for BigipGtmPoolMember {
    fn default() -> Self {
        Self {
            name: String::new(),
            description: String::new(),
            state: String::new(),
            member_order: String::new(),
            order: String::new(),
            service_port: String::new(),
            ratio: String::new(),
            monitor: String::new(),
            depends_on: String::new(),
            limit_max_bps: String::new(),
            limit_max_connections: String::new(),
            limit_max_pps: String::new(),
            static_target: String::new(),
        }
    }
}

/// Mirrors Python `BigipGtmProberPool`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmProberPool {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `load_balancing_mode`
    pub load_balancing_mode: String,
    /// `members`
    pub members: Vec<String>,
    /// `state`
    pub state: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmProberPool {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            load_balancing_mode: String::new(),
            members: Vec::new(),
            state: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmRegion`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmRegion {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `region_members`
    pub region_members: Vec<String>,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmRegion {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            region_members: Vec::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmRule`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmRule {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `source`
    pub source: String,
    /// `description`
    pub description: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmRule {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            source: String::new(),
            description: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmServer`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmServer {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `datacenter`
    pub datacenter: String,
    /// `monitor`
    pub monitor: String,
    /// `product`
    pub product: String,
    /// `addresses`
    pub addresses: Vec<String>,
    /// `virtual_servers`
    pub virtual_servers: Vec<String>,
    /// `description`
    pub description: String,
    /// `state`
    pub state: String,
    /// `prober_pool`
    pub prober_pool: String,
    /// `prober_preference`
    pub prober_preference: String,
    /// `prober_fallback`
    pub prober_fallback: String,
    /// `virtual_server_discovery`
    pub virtual_server_discovery: String,
    /// `expose_route_domains`
    pub expose_route_domains: String,
    /// `iq_allow_path`
    pub iq_allow_path: String,
    /// `iq_allow_service_check`
    pub iq_allow_service_check: String,
    /// `iq_allow_snmp`
    pub iq_allow_snmp: String,
    /// `limit_max_bps`
    pub limit_max_bps: String,
    /// `limit_max_connections`
    pub limit_max_connections: String,
    /// `limit_max_pps`
    pub limit_max_pps: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmServer {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            datacenter: String::new(),
            monitor: String::new(),
            product: String::new(),
            addresses: Vec::new(),
            virtual_servers: Vec::new(),
            description: String::new(),
            state: String::new(),
            prober_pool: String::new(),
            prober_preference: String::new(),
            prober_fallback: String::new(),
            virtual_server_discovery: String::new(),
            expose_route_domains: String::new(),
            iq_allow_path: String::new(),
            iq_allow_service_check: String::new(),
            iq_allow_snmp: String::new(),
            limit_max_bps: String::new(),
            limit_max_connections: String::new(),
            limit_max_pps: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmTopology`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmTopology {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `description`
    pub description: String,
    /// `order`
    pub order: String,
    /// `score`
    pub score: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmTopology {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            description: String::new(),
            order: String::new(),
            score: String::new(),
            range: None,
        }
    }
}

/// Mirrors Python `BigipGtmWideip`.
#[derive(Debug, Clone, PartialEq)]
pub struct BigipGtmWideip {
    /// `name`
    pub name: String,
    /// `full_path`
    pub full_path: String,
    /// `record_type`
    pub record_type: String,
    /// `pools`
    pub pools: Vec<String>,
    /// `aliases`
    pub aliases: Vec<String>,
    /// `pool_lb_mode`
    pub pool_lb_mode: String,
    /// `last_resort_pool`
    pub last_resort_pool: String,
    /// `description`
    pub description: String,
    /// `state`
    pub state: String,
    /// `failure_rcode`
    pub failure_rcode: String,
    /// `failure_rcode_response`
    pub failure_rcode_response: String,
    /// `failure_rcode_ttl`
    pub failure_rcode_ttl: String,
    /// `minimal_response`
    pub minimal_response: String,
    /// `persistence`
    pub persistence: String,
    /// `persist_cidr_ipv4`
    pub persist_cidr_ipv4: String,
    /// `persist_cidr_ipv6`
    pub persist_cidr_ipv6: String,
    /// `topology_prefer_edns0_client_subnet`
    pub topology_prefer_edns0_client_subnet: String,
    /// `ttl_persistence`
    pub ttl_persistence: String,
    /// `range`
    pub range: Option<crate::range::Range>,
}

impl Default for BigipGtmWideip {
    fn default() -> Self {
        Self {
            name: String::new(),
            full_path: String::new(),
            record_type: String::new(),
            pools: Vec::new(),
            aliases: Vec::new(),
            pool_lb_mode: String::new(),
            last_resort_pool: String::new(),
            description: String::new(),
            state: String::new(),
            failure_rcode: String::new(),
            failure_rcode_response: String::new(),
            failure_rcode_ttl: String::new(),
            minimal_response: String::new(),
            persistence: String::new(),
            persist_cidr_ipv4: String::new(),
            persist_cidr_ipv6: String::new(),
            topology_prefer_edns0_client_subnet: String::new(),
            ttl_persistence: String::new(),
            range: None,
        }
    }
}
