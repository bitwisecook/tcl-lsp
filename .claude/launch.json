{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "compiler-explorer",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "make explorer-build && cargo run -q -p tcl-cli --bin tcl -- explore --serve --port 8080"],
      "port": 8080
    }
  ]
}
